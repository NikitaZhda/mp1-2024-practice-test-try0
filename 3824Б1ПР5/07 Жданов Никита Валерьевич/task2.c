// Задача 2. Найти произведение натуральных чисел a и b
// Если a * b >= UINT_MAX, возвращать UINT_MAX

  if (a > 0 && b > 0 && a > UINT_MAX / b) {
    return UINT_MAX; // Проверка на переполнение
  }
  return a * b; // Возвращаем произведение
.
